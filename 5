from itertools import product
p = product('кума', repeat=5)
s = map(lambda x: ''.join(x),p)
q=0
for x in s:
    if ((x[0] == 'к') or (x[0]=='м')) and ((x[3]=='у') or (x[3]=='а')):
        q+=1
print(q)
